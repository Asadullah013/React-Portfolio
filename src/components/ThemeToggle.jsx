import React from 'react';

const ThemeToggle = ({ theme, toggleTheme }) => {
  // This component is optional since we have the toggle in Sidebar
  // You can remove the import from App.js if not needed
  return null;
};

export default ThemeToggle;